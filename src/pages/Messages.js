import React from 'react';
import Header from '../components/Header';

/**
 * Page de messagerie de l'utilisateur
 * @returns {JSX.Element}
 */
const Messages = () => (
  <div>
    <Header />
    <h1>Your Messages</h1>
    {/* Affichage des messages */}
  </div>
);

export default Messages;
